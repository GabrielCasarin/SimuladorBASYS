MT <M1> is

    Q = {q0, q1, q2, H}

    E = {'a',  'b'}
    G = {'a',  'b'}

    d = {
          (q0, a) -> (q1, <),    (q0, b) -> (q1, <), (q0, #) -> (q1, <),
          (q1, a) -> (q0, b),    (q1, b) -> (q0, a), (q1, #) -> (q2, >),
          (q2, a) -> (q2, >),    (q2, b) -> (q2, >), (q2, #) -> (H, #)
        }

    F = {H}

    I = {q0}

end <M1>;
