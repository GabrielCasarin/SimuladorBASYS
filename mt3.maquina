MT <M1> is

    Q = {q0, q1, q2, q3, q4, H}

    E = {'a',  'b', 'c'}
    G = {'a',  'b'}

    d = {
          (q0, #) -> (q0, <),    (q0, a) -> (q1, b),
          (q1, b) -> (q2, <),    (q2, b) -> (q3, a),
          (q3, b) -> (H, <),     (q3, a) -> (q4, <),
          (q4, #) -> (H, >),
          (H, a) -> (H, >),      (H, b) -> (H, >)
        }

    F = {H}

    I = {q0}

end <M1>;
